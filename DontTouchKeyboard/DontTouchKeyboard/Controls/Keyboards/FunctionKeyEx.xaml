<c:KeyboardBase
    x:Class="DontTouchKeyboard.Controls.Keyboards.FunctionKeyEx"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:DontTouchKeyboard.Controls.Keyboards"
    xmlns:c="using:DontTouchKeyboard.Controls"
    xmlns:ext="using:DontTouchKeyboard.Extensions"
    mc:Ignorable="d">

    <StackPanel Spacing="{ThemeResource KeyLargeSpacing}" Orientation="Horizontal">
        <StackPanel Spacing="{ThemeResource KeyDefaultSpacing}" Orientation="Horizontal">
            <c:KeyButton Key="Print" Size="Function" />
            <c:KeyButton Key="Scroll" Size="Function" LockVisibility="{x:Bind ext:CoreVirtualKeyStatesExtension.ToLockedVisibility(ScrollState), Mode=OneWay}" />
            <c:KeyButton Key="Pause" Size="Function" />
        </StackPanel>
    </StackPanel>
</c:KeyboardBase>
